using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Proj_Bomb_Orb : Proj_Bomb_Basic
{
    //[SerializeField] GameObject usingOrb; //플레이어가 사용할 투척 위성 >> orb

    //public override void Explode(Vector2 pos)
    //{
    //    GameObject newOrb = GameManager.Instance.poolManager.Get(usingOrb);
    //    newOrb.transform.position = pos;
    //    newOrb.transform.rotation = Quaternion.identity;

    //    if (hitEffect != null)
    //        GameManager.Instance.particleManager.GetParticle(hitEffect, transform.position, transform.rotation);
    //}

}
